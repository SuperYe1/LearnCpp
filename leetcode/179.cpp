#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

/*
179. 最大数

给定一组非负整数 nums，
重新排列每个数的顺序（每个数不可拆分）使之组成一个最大的整数。
*/

string largestNumber(vector<int> &nums)
{
    int n = nums.size();
    sort(nums.begin(),nums.end(),auto[&a,&b] )
}

int main()
{

    return 0;
}